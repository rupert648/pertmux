# pertmux configuration
# Copy to ~/.config/pertmux/pertmux.toml or pass via: pertmux -c ./config.toml

# How often (in seconds) to poll tmux panes and refresh the dashboard.
# refresh_interval = 2

# Command to run in a split pane when focusing a worktree (e.g. "opencode")
# default_agent_command = "opencode"

[agent.opencode]
# db_path = "~/.local/share/opencode/opencode.db"

[github]
token = ""  # Set PERTMUX_GITHUB_TOKEN env var or paste token here

[[project]]
name = "pertmux"
source = "github"
project = "rupert648/pertmux"
local_path = "/Users/rupert/Documents/personal/pertmux"
username = "rupert648"

[[project]]
name = "ferroxide"
source = "github"
project = "rupert648/ferroxide"
local_path = "/Users/rupert/Documents/personal/ferroxide"
username = "rupert648"
